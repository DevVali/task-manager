{ foreach [["welcome"], ["workspaces"], ["boards"]] as [$item] }
	<a href="/dash/{{ $item }}" class="{{ explode('.', $_name)[1] === $item ? 'bg-slate-200 text-black' : 'text-gray-700 hover:bg-gray-700 hover:text-white' }} block rounded-md px-3 py-2 text-base font-medium">
		{{ text("app.{$item}") }}
	</a>
{ /foreach }
