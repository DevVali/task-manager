{ foreach [["account"], ["logout"]] as [$item] }
	<a href="/account/{{ $item }}" class="block px-4 py-2 text-sm text-gray-700" role="menuitem" tabindex="-1">
		{{ text("app.{$item}") }}
	</a>
{ /foreach }
